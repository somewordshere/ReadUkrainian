{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "seed": "node scripts/build-content-seed.mjs",
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
